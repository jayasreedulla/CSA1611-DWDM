A <- c(18, 2, 20)
B <- c(22, 28, 10)
C <- c(20, 40, 40)

# Covariance between B and C
cov(B, C)

# Covariance matrix
cov(cbind(A, B, C))

# Correlation between B and C
cor(B, C)

# Correlation matrix
cor(cbind(A, B, C))
